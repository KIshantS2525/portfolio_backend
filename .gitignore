__pycache__/
*.py[cod]
.venv/
venv/
.env

# NOTE: biography.txt is NOT ignored.
# It is generated by `cd frontend && npm run export-bio`, but the backend
# deploys as a separate Vercel project that never runs the frontend build.
# If it isn't committed, the deployed chat starts with no biography loaded.
# Regenerate and commit it whenever you edit frontend/src/lib/content.ts.
entries.json
